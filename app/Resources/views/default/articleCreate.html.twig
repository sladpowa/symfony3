{% extends 'base.html.twig' %}
{% form_theme formArticle 'bootstrap_4_layout.html.twig' %}
{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-3 left-menu-container slideInLeft">
                {% include 'includes/leftmenu.html.twig' %}
            </div>
            <div class="col-sm-9 right-menu-container-80" id="right-menu-container">
                <h1 class="news-titre">CRÉATION D'UN ARTICLE : </h1>
                <hr>
                {{ form_start(formArticle) }}

                <label for="">Titre de l'article : </label>
                {{ form_widget(formArticle.title, {'attr':{'placeholder' : "Votre titre" }}) }}

                <label for="">Contenu de l'article : </label>
                {{ form_widget(formArticle.content, {'attr':{'placeholder':"Votre contenu"}}) }}

                <label for="">Image de l'article : </label>
                {{ form_widget(formArticle.image, {'attr': {'placeholder': "Url de votre image" }}) }}
                <br />
                <hr>
                <button type="submit" class="btn btn-outline-warning" >Publier</button>

                {{ form_end(formArticle) }}
                <hr>
            </div>
        </div>
    </div>
{% endblock %}