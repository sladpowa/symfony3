{% extends 'base.html.twig' %}
{% form_theme formTest 'bootstrap_4_layout.html.twig' %}
{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-3 left-menu-container slideInLeft">
                {% include 'includes/leftmenu.html.twig' %}
            </div>
            <div class="col-sm-9 right-menu-container-80" id="right-menu-container">
                <h1 class="h1-titre">Formulaire de test :</h1>
                <div>
                    {{ form_start(formTest) }}
                    {{ form_widget(formTest) }}
                    <br/>
                    <button type="submit" class="btn btn-outline-warning">Valider</button>
                    {{ form_end(formTest) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}