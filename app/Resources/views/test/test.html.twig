{% extends 'base.html.twig' %}

{% block body %}
<div class="container-fluid">
    <div class="row">
        <div class="col-sm-3 left-menu-container slideInLeft">
            {% include 'includes/leftmenu.html.twig' %}
        </div>
        <div class="col-sm-9 right-menu-container-80" id="right-menu-container">
            <h2 class="h2-titre">Test de formulaire</h2>
            {% for test in tests %}
            <h2 class="h2-titre">{{ test.name }}</h2>
                <a href="{{ path('test_form') }}" class="btn btn-outline-warning">Formulaire de test</a>
            {% endfor %}
        </div>
    </div>
</div>
{% endblock %}